# Yoda

A Python-based CLI for building, publishing, and deploying services to a VPS.

## Setup

```bash
python3 -m venv .venv
source venv/bin/activate
pip install .
```

## Usage

```bash
deploy service --tag inv-v1.2.3
deploy nextjs --tag app-v5.4.0
```
